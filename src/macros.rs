mod predicate;

#[cfg(test)]
mod testbench;

// print("\n".join([str((i, chr(i))) for i in range(128)]))

/*
[[macro_content]]

macro content is case insensitive, and all the whitespaces are ignored.

a-z A-Z 0-9 `=` `,` ` ` `_` `/`

32, 44, 47, 48 ~ 57, 61, 65 ~ 90, 95, 97 ~ 122
*/